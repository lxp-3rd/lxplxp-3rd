import type { AuthRequest } from '../types';

interface RegisterFormProps {
  data: AuthRequest;
}

export function RegisterForm({ data }: RegisterFormProps) {
  return (
    <div>
      {/* TODO: RegisterForm 구현 예정 */}
    </div>
  );
}
