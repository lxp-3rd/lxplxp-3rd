import type { AuthRequest } from '../types';

interface LoginFormProps {
  data: AuthRequest;
}

export function LoginForm({ data }: LoginFormProps) {
  return (
    <div>
      {/* TODO: LoginForm 구현 예정 */}
    </div>
  );
}
