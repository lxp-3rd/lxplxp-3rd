import type { InstructorApplication } from '../types';

interface ApplicationCardProps {
  data: InstructorApplication;
}

export function ApplicationCard({ data }: ApplicationCardProps) {
  return (
    <div>
      {/* TODO: ApplicationCard 구현 예정 */}
    </div>
  );
}
