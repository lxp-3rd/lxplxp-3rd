spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

lxp:
  auth:
    jwt:
      secret: lxp-jwt-secret-key-for-test-environment-1234567890
